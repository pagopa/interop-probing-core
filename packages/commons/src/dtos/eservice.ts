export enum EserviceInteropState {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum EserviceTechnology {
  REST = "REST",
  SOAP = "SOAP",
}

export enum EserviceStatus {
  OK = "OK",
  KO = "KO",
}
