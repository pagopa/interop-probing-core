import { defineConfig, mergeConfig, defineProject } from "vitest/config";
import baseConfig from "./vitest.config.js";

export { defineConfig, mergeConfig, defineProject };
export default baseConfig;
